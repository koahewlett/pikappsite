'use client';
import { useState } from 'react';
import { supabase } from '@/lib/supabase';
export function AuthForm(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function signIn(){const {error}=await supabase.auth.signInWithPassword({email,password});setMsg(error?error.message:'Logged in. Go to /dashboard.')}async function signUp(){const {error}=await supabase.auth.signUp({email,password});setMsg(error?error.message:'Check your email to confirm signup.')}async function reset(){const {error}=await supabase.auth.resetPasswordForEmail(email);setMsg(error?error.message:'Password reset email sent.')}return <div className="glass mx-auto max-w-md rounded-[2rem] p-8"><h1 className="text-3xl font-black">Member Access</h1><input className="mt-6 w-full rounded-2xl bg-black/40 p-4" placeholder="Email" onChange={e=>setEmail(e.target.value)}/><input className="mt-3 w-full rounded-2xl bg-black/40 p-4" placeholder="Password" type="password" onChange={e=>setPassword(e.target.value)}/><div className="mt-5 grid gap-2"><button onClick={signIn} className="rounded-full bg-gold p-3 font-bold text-ink">Log In</button><button onClick={signUp} className="rounded-full bg-white/10 p-3">Sign Up</button><button onClick={reset} className="text-sm text-white/60">Forgot Password</button></div>{msg&&<p className="mt-4 text-gold">{msg}</p>}</div>}
