'use client';
import { useState } from 'react';
import { supabase } from '@/lib/supabase';
const steps = ['Personal Information','ASU Information','Involvement','Social Links','Submit'];
export function ApplicationForm(){const [step,setStep]=useState(0);const [status,setStatus]=useState('');const [form,setForm]=useState<Record<string,string>>({});const fields=[['first_name','last_name','phone','email'],['major','graduation_year','hometown'],['sports','clubs','leadership_positions'],['instagram','linkedin'],[]][step];async function submit(){setStatus('Submitting...');const {error}=await supabase.from('rush_applications').insert([{...form,status:'new'}]);setStatus(error?error.message:'Application submitted. Our rush team will reach out soon.');}return <div id="apply" className="glass mx-auto max-w-3xl rounded-[2rem] p-6 md:p-10"><div className="mb-6 flex flex-wrap gap-2">{steps.map((s,i)=><button key={s} onClick={()=>setStep(i)} className={`rounded-full px-3 py-1 text-xs ${i===step?'bg-gold text-ink':'bg-white/10 text-white/60'}`}>{i+1}. {s}</button>)}</div><h2 className="mb-6 text-3xl font-black">Rush Application</h2>{fields.length>0?<div className="grid gap-4 md:grid-cols-2">{fields.map(f=><label key={f} className="text-sm capitalize text-white/70">{f.replaceAll('_',' ')}<input className="mt-2 w-full rounded-2xl border border-white/10 bg-black/30 px-4 py-3 text-white outline-none focus:border-gold" value={form[f]||''} onChange={e=>setForm({...form,[f]:e.target.value})}/></label>)}</div>:<div className="rounded-3xl bg-white/5 p-5 text-white/75">Review your information, then submit your application to Pi Kappa Phi at ASU.</div>}<div className="mt-8 flex justify-between"><button disabled={step===0} onClick={()=>setStep(step-1)} className="rounded-full bg-white/10 px-5 py-3 disabled:opacity-30">Back</button>{step<4?<button onClick={()=>setStep(step+1)} className="rounded-full bg-gold px-5 py-3 font-bold text-ink">Next</button>:<button onClick={submit} className="rounded-full bg-gold px-5 py-3 font-bold text-ink">Submit</button>}</div>{status&&<p className="mt-4 text-gold">{status}</p>}</div>}
